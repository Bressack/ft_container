#include <vector>
#include <iostream>

# define MAX_SIZE 100

int		main(void)
{
    std::vector<long> d;

    d.push_back(2);

    for (int i = 0; d.size() < MAX_SIZE; i++)
        for (int j = 0; j < d.size(); j++)
            
    return (0);
}